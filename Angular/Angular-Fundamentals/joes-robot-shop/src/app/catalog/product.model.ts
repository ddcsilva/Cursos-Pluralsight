// Definição da interface de produtos
export interface IProduct {
    id: number;
    descricao: string;
    nome: string;
    nomeImagem: string;
    categoria: string;
    preco: number;
    desconto: number;
}